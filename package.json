{
  "name": "aura-standalone",
  "version": "1.0.0",
  "description": "Standalone backend for the AURA assistant app — proxies requests to the Anthropic API using a real, server-side API key.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
